spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_URL:http://polar-keycloak:8080}/realms/PolarBookshop
  cloud:
    config:
      bus:
        enabled: true
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:polar-kafka:19092}
