apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  labels:
    app: inventory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "9004"
      labels:
        app: inventory-service
    spec:
      containers:
        - name: inventory-service
          image: pxloc97/inventory-service:0.0.1-SNAPSHOT
          imagePullPolicy: Always
          lifecycle:
            preStop:
              exec:
                command: ["/bin/sh", "-c", "sleep 5"]
          ports:
            - containerPort: 9004
          env:
            - name: BPL_JAVA_NMT_ENABLED
              value: "false"
            - name: BPL_JVM_THREAD_COUNT
              value: "50"
            - name: JAVA_TOOL_OPTIONS
              value: "-XX:MaxDirectMemorySize=128M -XX:+AlwaysPreTouch -XX:MaxGCPauseMillis=50 -XX:+UseStringDeduplication"
            - name: OTEL_SERVICE_NAME
              value: "inventory-service"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://grafana-alloy:4318"
            - name: MANAGEMENT_OPENTELEMETRY_TRACING_EXPORT_OTLP_ENDPOINT
              value: "http://grafana-alloy:4318/v1/traces"
            - name: OTEL_LOGS_EXPORTER
              value: "otlp"
            - name: OTEL_EXPORTER_OTLP_LOGS_ENDPOINT
              value: "http://grafana-alloy:4318/v1/logs"
            - name: MANAGEMENT_OPENTELEMETRY_LOGGING_EXPORT_OTLP_ENDPOINT
              value: "http://grafana-alloy:4318/v1/logs"
            - name: OTEL_METRICS_EXPORTER
              value: "none"
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-service"
            - name: SPRING_R2DBC_URL
              value: "r2dbc:postgresql://polar-postgres-inventory/polardb_inventory"
            - name: SPRING_FLYWAY_URL
              value: "jdbc:postgresql://polar-postgres-inventory/polardb_inventory"
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: "polar-kafka:19092"
            - name: REDIS_HOST
              value: "polar-redis"
            - name: MTLS_ENABLED
              value: "false"
            - name: MTLS_KEY_STORE
              value: "/etc/mtls/keystore.p12"
            - name: MTLS_KEY_STORE_PASSWORD
              value: ""
            - name: MTLS_KEY_STORE_TYPE
              value: "PKCS12"
            - name: MTLS_TRUST_STORE
              value: "/etc/mtls/truststore.p12"
            - name: MTLS_TRUST_STORE_PASSWORD
              value: ""
            - name: MTLS_TRUST_STORE_TYPE
              value: "PKCS12"
            - name: BACKEND_MTLS_CLIENT_AUTH
              value: "need"
          volumeMounts:
            - name: inventory-service-volume
              mountPath: /workspace/config
            - name: inventory-service-volume-secret
              mountPath: /workspace/secrets/inventory-service
              readOnly: true
            - name: mtls-certs
              mountPath: /etc/mtls
              readOnly: true
      volumes:
        - name: inventory-service-volume
          configMap:
            name: inventory-service
        - name: inventory-service-volume-secret
          secret:
            secretName: inventory-service
        - name: mtls-certs
          secret:
            secretName: inventory-service-mtls-certs
            optional: true
